package cyphers;

import common.Constants;

public class Caesar {
	
	/*
 		String alphabet = Constants.ALPHABET_DE;
		int key = 23;
		
		String encryptedAlphabet = alphabet.substring(key);
		encryptedAlphabet += alphabet.substring(0, key);
		
		System.out.println(encryptedAlphabet);
	 */

}
